// import createGallery from '/src/js/main.js';

// const form = document.querySelector(".form");
// const search = document.querySelector(".input-home");
// const API_URL_SEARCH = `${BASE_URL}?api_key=${API_KEY}&query=${data}&page=${page}`;

// form.addEventListener("submit", (e) => {
//     e.preventDefault();

//     const apiSearchUrl = `${API_URL_SEARCH}${data}`;
//     if (data) {
//         createGallery(apiSearchUrl);

//       search.value = "";
//     }
//   });
